// https://atcoder.jp/contests/abc165/tasks/abc165_d

#include <cstdio>

using namespace std;

#define REP(i,n)   for(int i=0; i<(int)(n); i++)
#define FOR(i,b,e) for(int i=(b); i<=(int)(e); i++)

const int A_MAX = 1e6;
const int B_MAX = 1e12;
const int N_MAX = 1e12;

int A, B, N;

void solve() {
  int ans = 0;
  printf("%d\n", ans);
}

void input() {
  scanf("%d%d%d", &A, &B, &N);
}

int main() {
  input();
  solve();
  return 0;
}
